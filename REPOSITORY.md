# Repository note

This repository layout was designed around the Limiar 3D single-file constraint. `limiar3d.lisp` is the engine; every other file exists to make that engine easier to install, test, learn, demonstrate, and distribute as source.

The documentation organization is intentionally bilingual and code-first.
